cmd-mapping-desc =  Crie ou carregue um mapa e te teleporta para ele.

cmd-mapping-help = Uso: mapeamento [MapID] [Path] [Grid]

cmd-mapping-server =  Só os jogadores podem usar este comando.

cmd-mapping-error = Um erro ocorreu ao criar o novo mapa.

cmd-mapping-try-grid =  Não foi possível carregar o arquivo como um mapa, tentando carregar o arquivo como uma grade...

cmd-mapping-success-load =  Criado mapa não inicializado a partir de arquivo { $path } com identificação { $mapId }.

cmd-mapping-success-load-grid =  Carregado grade não inicializada do arquivo { $path } em um novo mapa com identidade { $mapId }.

cmd-mapping-success =  Criado mapa não iniciado com identificação { $mapId }.

cmd-mapping-warning = O servidor está usando uma compilação de depuração, você está arriscando perder suas mudanças.

# duplicate text from engine load/save map commands.
# I CBF making this PR depend on that one.
cmd-mapping-failure-integer = { $arg }  Não é um inteiro válido.

cmd-mapping-failure-float = { $arg }  Não é um carro válido.

cmd-mapping-failure-bool = { $arg }  Não é um bool válido.

cmd-mapping-nullspace =  Não pode carregar no mapa 0.

cmd-hint-mapping-id = [MapID]

cmd-mapping-hint-grid = [Grid]

cmd-hint-mapping-path = [Path]

cmd-mapping-exists =  Mapa { $mapId }  Já existe.
